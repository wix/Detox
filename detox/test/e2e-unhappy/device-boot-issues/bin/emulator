#!/usr/bin/env bash

source "$(dirname "$0")/helpers.sh"

if [ -z "$DETOX_UNHAPPY_EMULATOR" ]; then
  printf "\$DETOX_UNHAPPY_EMULATOR is not set, exiting...\n"
  exit 2
fi

if has_arg "--no-audio" "$@"
then
  simulate_error
else
  $DETOX_UNHAPPY_EMULATOR $@
fi

