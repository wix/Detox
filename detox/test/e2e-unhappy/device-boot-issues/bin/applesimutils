#!/usr/bin/env bash

source "$(dirname "$0")/helpers.sh"

if [ -z "$DETOX_UNHAPPY_APPLESIMUTILS" ]; then
  printf "\$DETOX_UNHAPPY_APPLESIMUTILS is not set, exiting...\n"
  exit 2
fi

if has_arg "--byId" "$@"
then
  simulate_error
else
  $DETOX_UNHAPPY_APPLESIMUTILS $@
fi

