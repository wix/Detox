describe('User Notifications', () => {
  beforeEach((done) => {
    simulator.reloadReactNativeApp(done);
  });

  beforeEach(() => {
    element(by.label('Sanity')).tap();
  });

  it('receive local notification', () => {

  });
});
