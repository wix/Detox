{
  "name": "demo-plugin",
  "version": "19.13.0-feature.is-hittable-check.0",
  "private": true,
  "scripts": {
    "test:plugin": "detox test --configuration plugin -l verbose"
  },
  "devDependencies": {
    "detox": "^19.13.0-feature.is-hittable-check.0",
    "jest": "^28.0.0"
  },
  "detox": {
    "test-runner": "jest",
    "configurations": {
      "plugin": {
        "binaryPath": "my/random/path",
        "device": {
          "foo": "bar"
        },
        "name": "plugin-example",
        "type": "./driver"
      }
    }
  }
}
