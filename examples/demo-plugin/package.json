{
  "name": "demo-plugin",
  "version": "17.11.4",
  "private": true,
  "scripts": {
    "test:plugin": "detox test --configuration plugin -l verbose"
  },
  "devDependencies": {
    "detox": "^17.11.4",
    "jest": "^26.5.0"
  },
  "detox": {
    "test-runner": "jest",
    "configurations": {
      "plugin": {
        "binaryPath": "my/random/path",
        "device": {
          "foo": "bar"
        },
        "name": "plugin-example",
        "type": "./driver"
      }
    }
  }
}
