{
  "name": "@detox-remote/driver-ios",
  "version": "0.1.0",
  "private": true,
  "description": "The ios.simulator driver (spec 015): CoreSimulator behind the driver seam — the pool, simctl/applesimutils, the injected launch, the wipe, one app gateway per booted simulator",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@detox-remote/core": "workspace:*",
    "@detox-remote/protocol": "workspace:*"
  },
  "devDependencies": {
    "@detox-remote/server": "workspace:*",
    "typescript": "^5.3.0"
  },
  "license": "MIT"
}
